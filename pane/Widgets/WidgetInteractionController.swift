import AppKit

final class WidgetInteractionController {
    func attach(to window: NSWindow) {
        // Placeholder for drag/resize interaction states in later phases.
        window.acceptsMouseMovedEvents = true
    }
}
